services:
  - type: web
    name: fit01
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py --port $PORT
    envVars:
      - key: AI_API_KEY
        sync: false
